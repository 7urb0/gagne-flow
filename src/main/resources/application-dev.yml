# GagneFlow 本地开发环境配置
# 使用方式: --spring.profiles.active=dev 或在 IDE Run Configuration 中设置
# 此文件包含开发友好的默认值，不适用于生产环境

spring:
  datasource:
    # 开发环境使用 docker-compose 中定义的默认凭据
    username: gagneflow
    password: gagneflow123

# 开发环境关闭 JWT secret 校验（生产环境会强制要求）
# gagneflow:
#   auth:
#     jwt-secret: gagneflow-demo-secret-key-2026-is-32-bytes
