# GagneFlow 生产环境配置
# 敏感值通过环境变量注入，此处无默认值确保生产安全

gagneflow:
  auth:
    jwt-secret: ${JWT_SECRET}

spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
